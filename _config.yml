encoding: UTF-8
highlighter: rouge

exclude:
  - Gemfile
  - Gemfile.lock
  - bin
  - _posts/Journey/letters
  - _posts/Summary/daily
  - _posts/Summary/quarter
  - _posts/Slides
  - xyz
  - slides
  - vendor
  - rakefile

kramdown:
  syntax_highlighter: rouge
  input: GFM

  # Rouge Highlighter in Kramdown › //kramdown.gettalong.org/syntax_highlighter/rouge.html
  # span, block element options fall back to global
  syntax_highlighter_opts:
    #line_numbers: true # bad idea, spans don't need linenos and would inherit this option
    span:
      line_numbers: false
    block:
      line_numbers: false
      start_line: 1